name: Deploy to GitHub Pages

on:
  push:
    branches:
      - main

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
    - name: Set up Node.js
      uses: actions/setup-node@v2
      with:
        node-version: '16.x'
    - name: Install dependencies
      run: npm install
    - name: Install Angular CLI
      run: npm install -g @angular/cli
    - name: Congig git
        git config --global user.email "mksbnt@icloud.com"
        git config --global user.name "mksbnt"
    - name: Build and deploy
      run: |
        ng build --configuration development --base-href=/mps/
        npx angular-cli-ghpages --dir=dist/mps --no-silent
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
