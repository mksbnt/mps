import { UpdatePointNumbersPipe } from './update-point-numbers.pipe';

describe('UpdatePointNumbersPipe', () => {
  it('create an instance', () => {
    const pipe = new UpdatePointNumbersPipe();
    expect(pipe).toBeTruthy();
  });
});
